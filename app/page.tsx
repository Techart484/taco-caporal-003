import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function HomePage() {
  return (
    <div className="min-h-screen">
      {/* Hero Section */}
      <section className="py-20 px-4 text-center">
        <h1 className="text-5xl font-bold mb-4">Taste the Tradition: Authentic Tacos & Mexican Delights</h1>
        <p className="text-xl text-muted-foreground mb-8 max-w-2xl mx-auto">
          Order your favorites online for pickup or delivery and experience the true flavors of Mexico.
        </p>
        <Button size="lg">View Our Full Menu</Button>
      </section>

      {/* Features/Services Section */}
      

      
    </div>
  );
}